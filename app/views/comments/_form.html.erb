<%= form_for([@post,@post.comments.build]) do |f|%>
	<p>
		<%= f.label :Name %><br>
	    <%= f.text_field :name %>
	</p>

	<p>
		<%= f.label :Comment %><br>
	    <%= f.text_area :body %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end%>